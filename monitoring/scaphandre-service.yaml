apiVersion: v1
kind: Service
metadata:
  name: scaphandre
  namespace: monitoring
  labels:
    app.kubernetes.io/name: scaphandre
    release: prometheus
spec:
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
  selector:
    app.kubernetes.io/name: scaphandre
