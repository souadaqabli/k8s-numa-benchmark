apiVersion: batch/v1
kind: Job
metadata:
  name: native-job-rand-time
spec:
  parallelism: 4
  completions: 4
  template:
    metadata:
      labels:
        app: native-rand-time
    spec:
      nodeName: sdn2
      containers:
      - name: benchmark
        image: memory-benchmark:v_numa4
        command: ["/bin/bash", "-c"]
        args:
          - |
            export OUTPUT_DIR=/app/results/native_rand_${HOSTNAME}_time
            mkdir -p $OUTPUT_DIR
            export PATTERN='random'
            export EXPERIMENT_MODE='time'
            
            python3 /app/scripts/script_controlled.py --output-dir "$OUTPUT_DIR"
        securityContext: 
          privileged: true
        resources: 
          requests: { cpu: "1", memory: "3Gi" }
          limits: { cpu: "1", memory: "10Gi" }
        volumeMounts: 
          - name: results-volume
            mountPath: /app/results
          - name: script-python-volume
            mountPath: /app/scripts/script_controlled.py
            subPath: script_controlled.py
          - name: script-python-volume
            mountPath: /app/scripts/mem_stress_controlled.py
            subPath: mem_stress_controlled.py
      restartPolicy: Never
      volumes: 
        - name: results-volume
          hostPath: 
            path: /home/sdnuser/k8s-numa-benchmark/results
            type: DirectoryOrCreate
        - name: script-python-volume
          configMap:
            name: script-benchmark-controlled